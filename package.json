{
  "name": "@flashist/fbuildscripts",
  "version": "0.0.2",
  "license": "MIT",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/flashist/fcore"
  },
  "main": "gulpfile.js",
  "scripts": {
    "build-fbuildscripts": "",
    "build": "gulp",
    "publish:patch": "npm version patch && npm run build && cd ./dist && npm publish",
    "publish:minor": "npm version minor && npm run build && cd ./dist && npm publish",
    "publish:major": "npm version major && npm run build && cd ./dist && npm publish"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-shell": "^0.8.0",
    "require-dir": "^1.2.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.2"
  }
}
